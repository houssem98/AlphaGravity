# Claude Code — Execute the Beat Top ChatGPT Finance Quick Answer Roadmap

Read `BEAT_TOP_CHATGPT_FINANCE_QUICK_ANSWER_ROADMAP.md` completely, then EXECUTE it in the current AlphaGravity repository. This is a real implementation task, not a review.

- Inspect existing code first.
- Actually modify code and add regression/integration tests for every fix.
- Run relevant tests after each phase; fix failures and rerun.
- Never delete, skip, weaken, or loosen tests.
- Never hard-code a small company allowlist.
- Never invent live, browser, timing, or benchmark results.
- Never turn Quick Answer into Deep Research.
- Never declare completion while a known correctness bug remains.

Priority: citation/provenance → exact SEC HTML/index links → strict periods → safe finance math → universal entity resolution → Company skill → Sentiment skill → scope-aware sets → provider failure semantics → latency forensics → Answer Contract → answer quality → golden benchmark → blind head-to-head benchmark → live/browser validation → red-team.

Use real sec.gov validation when possible. Instrument every pipeline boundary and identify the actual latency bottleneck. Never claim exhaustive set coverage without a verified complete universe.

Run all available backend/frontend tests, typecheck, build, finance/Quick Answer/skill evals, period/math/planning/scope/provenance tests, live SEC/skill tests, performance, browser E2E, head-to-head benchmark, and gate-guard.

For every defect: REPRODUCE → FAILING TEST → FIX → REGRESSION TEST → RERUN.

Create/update `docs/quick-answer/FINAL_BEAT_TOP_CHATGPT_VERIFICATION.md`.

For every gate report PASS / PARTIAL / BLOCKED / UNVERIFIED with exact command, count, exit code, latency p50/p95 where applicable, and evidence.

START NOW. KEEP EXECUTING, TESTING, FIXING, AND RERUNNING UNTIL ALL EXECUTABLE ROADMAP GATES PASS.
