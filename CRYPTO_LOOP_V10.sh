#!/bin/bash
# Crypto V10 NEWS VISUAL loop — self-paced, read roadmap and execute first unchecked task

Read docs/CRYPTO_V10_NEWS_VISUAL_ROADMAP.md and follow its doctrine and hard rules exactly. Do only the first unchecked [ ] task. Doctrine recap: TRUTH intact — 7-day horizon, newest-first, strict per-coin title match, 40-outlet whitelist ALL STAY; outlet-feed items pass same filters; honest empty stays; NO fake/placeholder images. NO new serverless files — api/news.ts only, no new npm deps, no page scraping (RSS + embedded media URLs only). Outlet feeds parallel w/ Google RSS, 6s timeout each, partial-tolerant (dead feed never 500s). Dedupe by normalized title. TN path byte-identical; legacy /api/news?q= unchanged. UI: our design tokens only, images lazy object-cover, onError hides img. Verify per task: market-ui typecheck 0 + vercel --prod (repo root) + prod curl real numbers + TN regression (BIAT news + board + intraday 200s) + audit green (spot 200/200 MISMATCH 0) + leak check (GRAM zero cross-asset titles). Flip task [x], one Progress-log line real numbers, commit on roadmap/world-class (git commit -F file if quotes — rtk mangles them). PERSISTENCE: never end loop on usage/rate limit or 429/overload — stop consuming tokens, ScheduleWakeup 3600s with this exact /loop prompt, on wake re-read ledger and resume same task from last verified step (ledger + Progress log = recovery state); chain hourly wakeups. Only stop for: no [ ] remaining, or blocked on user-only input — say what's needed and still ScheduleWakeup 3600s.
