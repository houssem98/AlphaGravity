#!/bin/bash
# Crypto V9 NEWS TAB TERMINAL loop — self-paced, read roadmap and execute first unchecked task

Read docs/CRYPTO_V9_PROFILE_PAGES_ROADMAP.md and follow its doctrine and hard rules exactly. Do only the first unchecked [ ] task. Doctrine recap: TRUTH intact — NT-1 horizon (days=7 newest-first) and NT-2 strict per-coin title match STAY; widening sources must not reintroduce cross-contamination or stale news; honest empty stays for uncovered coins. Whitelist widens to CURATED named outlets only (never removed entirely — SEO spam stays out). NO new serverless files — server edits in api/news.ts only, no new npm deps, no images/scraping (Google RSS has no thumbnails). TN news path byte-identical; legacy /api/news?q= unchanged. Terminal header uses cryptoStore livePrice (one-source rule, no new fetch). Verify per task: market-ui typecheck 0 + vercel --prod (repo root) + prod curl real numbers + TN regression (BIAT news + board + intraday 200s) + audit green (spot 200/200 MISMATCH 0) + leak spot-check (GRAM zero cross-asset titles). Flip task [x], one Progress-log line real numbers, commit on roadmap/world-class (git commit -F file if quotes — rtk mangles them). PERSISTENCE: never end loop on usage/rate limit or 429/overload — stop consuming tokens, ScheduleWakeup 3600s with this exact /loop prompt, on wake re-read ledger and resume same task from last verified step (ledger + Progress log = recovery state); chain hourly wakeups. Only stop for: no [ ] remaining, or blocked on user-only input — say what's needed and still ScheduleWakeup 3600s.
