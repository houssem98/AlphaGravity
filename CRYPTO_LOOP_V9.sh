#!/bin/bash
# Crypto V9 PROFILE PAGES loop — self-paced, read roadmap and execute first unchecked task

Read docs/CRYPTO_V9_PROFILE_PAGES_ROADMAP.md and follow its doctrine and hard rules exactly. Do only the first unchecked [ ] task. Doctrine recap: Terminal aesthetic (CSS .terminal-profile dark bg #0a0e27, bright #00ff88/#0088ff, monospace, grid borders rgba(0,255,136,0.2)). Micro-status blocks = one-liner code blocks (price|vol|mcap|24h%|supply, no charts). News feed snippet = newest 1-2 titles V8 (60ch clip). Per-coin profile = name/ticker/genesis/categories/description[120]/links/status/news. NO new serverless files — all UI in apps/market-ui/src/pages/ or components/, profile data from existing blob APIs (markets.ts, news.ts). Reuse V7/V8 only, keyless, curl-verify profile shape before coding. Verify per task: market-ui typecheck 0 + vercel --prod (repo root) + curl /api/crypto/markets real shape + audit green (spot 200/200 MISMATCH 0) + TN board/intraday 200s + cross-check ProfileCard name/status/news real on 10-coin sample. Flip task [x], one Progress-log line with real numbers, commit on roadmap/world-class (normal commits, not -F file). PERSISTENCE: stop consuming tokens on usage/rate limit, ScheduleWakeup 3600s with this /loop prompt, resume same task from last ledger/log state (recovery state). Only stop for: no [ ] remain, or blocked on user input — then say what's needed + ScheduleWakeup 3600s.
