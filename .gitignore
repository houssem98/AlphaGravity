# ── Dependencies ─────────────────────
node_modules/
.venv/
.venv-hr/
__pycache__/
*.pyc
*.pyo

# ── Build artifacts ──────────────────
dist/
build/
.next/
out/
*.egg-info/

# ── Environment ──────────────────────
.env
.env.local
.env.*.local

# ── IDE ──────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ───────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Docker ───────────────────────────
docker-compose.override.yml

# ── Logs ─────────────────────────────
*.log
logs/

# ── Coverage ─────────────────────────
coverage/
htmlcov/
.coverage
.pytest_cache/
.nyc_output/
.vercel

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# ── External reference repos (not part of this codebase) ──
# Clone separately if needed — see README.
FinceptTerminal-main/
TradingAgents-main/
AionUi/
caveman/
ponytail/

# ── Claude tool runtime ──
.claude/scheduled_tasks.lock
.env.production
.codegraph/

# Secrets / local config (never commit — keys live here)
.claude/settings.json
.claude/settings.local.json
scripts/hermes.bat
.hyperresearch/config.toml
.hyperresearch/hyperresearch.db

# ── Regenerable / local-only ─────────
graphify-out/20*/
graphify-out/cache/
services/gravity-api/data/
test-results/
research/
personal-website/
gemini-code-*.md
apps/market-ui/diag-tmp.mjs
apps/market-ui/r17.txt
*.exe
attachments/

# Generated run artifacts — audit/eval output, not source.
services/gravity-api/results/

# Personal ticket PDF that lives in this folder; carries a phone number and a
# live check-in code, so it must never enter git history.
Invitation.pdf
