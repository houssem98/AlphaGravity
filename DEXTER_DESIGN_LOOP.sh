# DEXTER_DESIGN_LOOP — feed the last line of this file to /loop:
#   /loop $(tail -1 DEXTER_DESIGN_LOOP.sh)
#
# Ledger:  docs/DEXTER_DESIGN_ROADMAP.md   (faults F1-F12, doctrine, DD-1..DD-14)
# Scope:   presentation only. The agent's reasoning is DONE and correct
#          (docs/AI_TRADING_AGENT_ROADMAP.md, DX-1..DX-16). Never edit the
#          pipeline from this loop — a design task that seems to need a smarter
#          answer needs a prompt contract (DD-8), not new reasoning.
Read docs/DEXTER_DESIGN_ROADMAP.md and follow its doctrine and hard rules exactly. Do only the first unchecked [ ] task in Section 7; the task's spec text is the requirement, the Section 6 regression rows it names are the acceptance tests. TRUTH: never invent a token, class, prop, or number — read the actual anchors in Section 2 first (src/index.css:15-31 for --bg/--surface/--surface-2/--line/--line-strong/--text/--text-2/--text-3/--text-4/--accent/--accent-ink/--up/--down/--flat, tailwind.config.js:7-61 for the colour names, Archivo/Archivo Narrow/Martian Mono families, the label-11px/data-12px/body-13px scale, the 8px spacing steps and the radii capped at xl=8px, src/components/trading/Assistant.tsx for the component under repair, src/services/dexterTrust.ts chipPropsFor, src/services/gridTrace.ts stepGlyph+traceSummary, src/services/dexterTools.ts AgentReply+DexterCitation, api/agent/[fn].ts for the reply payload and the NDJSON send/finish seam) and confirm a prop exists on the payload before you render it. DESIGN SERVES VERIFICATION: every visual decision must make it EASIER to tell a supported claim from an unsupported one, so a citation must be reachable from the claim in one click, an uncited figure must be marked where it sits in the prose and never inside a cited sentence, a fabricated citation must render red above the answer, and no styling may ever make an unsupported figure look supported or cosmetically upgrade a grade — prettiness that hides provenance is a regression, and the UI never asserts more confidence than the payload carries. Never display a value the server did not send: provider, model, ms, step count, grade, score, rounds and reasons all ship on the reply, so render those and delete anything hardcoded (the `Powered by Gemini 3.1 Pro` string at Assistant.tsx:540 is what inventing one looks like — the server returns deepseek/deepseek-v4-flash). TOKENS ONLY: no hex literal, no text-[Npx], no rounded-2xl (16px is off-scale — the config only overrides sm/DEFAULT/md/lg/xl), and no prose-* class anywhere (@tailwindcss/typography is NOT installed, tailwind.config.js:72 is plugins:[require("tailwindcss-animate")], so every prose class at Assistant.tsx:461 currently compiles to nothing) — style react-markdown through an explicit components map instead; grep your own diff for those four patterns before you call a task done. DEGRADE HONESTLY: a reply missing citations/trust/steps renders text only with no empty shells and no crash, old localStorage sessions (Assistant.tsx:372-381, last 40 messages) must still render, an unknown dexter-* fenced block falls back to a code block and never throws, and absent structured blocks leave the prose path byte-identical. Reuse the spine — chipPropsFor, stepGlyph, traceSummary, AgentReply, DexterCitation, motion/AnimatePresence, react-markdown@10 + remark-gfm@4 are all already present; extend them, never fork a parallel renderer, and add no npm dependency without reading package.json first (prefer a components map over a plugin). HARD CONSTRAINTS: Vercel Hobby caps functions at 12 and apps/market-ui/api is full, so add NO new API route — server-side prompt work rides the existing api/agent/[fn].ts dispatcher; apps/market-ui/vercel.json rewrites /api/* to Fly behind a (?!tn/|agent/) negative lookahead, do not touch it; Vercel Node ESM needs explicit .js extensions on relative imports from src/ reachable by api/ (builds fine, 500s at request time); erasableSyntaxOnly forbids constructor parameter properties; only DEEPSEEK_API_KEY is live (Gemini quota-dead, Anthropic 401, Groq 401); VITE_API_URL is commented out in .env.production so market-server is unreachable from prod; prod alias is https://market-ui-self.vercel.app and the chat body shape is {messages,asset:{symbol,isTN,isCrypto,name},stream} — a flat symbol yields a null ctx, an empty tool belt and a misleading probe. A task is done only when: its numbered Section 6 rows are green via npx vitest run (from apps/market-ui/), npx tsc --noEmit -p tsconfig.app.json is 0 errors, ALL existing suites stayed green (the dexter*, taLevels, drawGate, gridTrust, gridTrustRunner, gridLessons, gridRunStore, gridTrace, gridResearch.sources and EdgarLink suites — 353 passing at ledger open), vercel --prod ran from repo root, and you POSTed the real payload above to the live panel and confirmed from the actual 200 response that the change is visible (paste the citation count, trust grade and per-step ms into the log — a fixture proves the component, only prod proves the product). Then flip the ledger line to [x], append ONE Section 8 Progress-log line with REAL numbers (test counts, probe status codes, measured ms, grade, uncited counts — no adjectives), and commit on roadmap/world-class with message "feat(dexter-ui): DD-n <what> — <verify numbers>" (git commit -F file if the message has quotes — rtk mangles them). Never git push unless asked. PERSISTENCE: never end the loop on usage/rate limit or 429/overload — stop consuming tokens immediately, ScheduleWakeup 3600s with this exact /loop prompt, and on wake re-read the ledger + Progress log and resume the same task from its last verified step; log partial progress BEFORE long or risky operations; chain hourly wakeups as needed. Only stop for: no [ ] remaining in Section 7, or a task blocked on user-only input (credentials/infra) — then say exactly what is needed, ledger-note it, skip to the next unblocked [ ] task if one exists, and only if ALL remaining are blocked ScheduleWakeup 3600s so the loop resumes when unblocked.
