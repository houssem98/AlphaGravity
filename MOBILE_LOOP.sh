# MOBILE_LOOP — feed the last line of this file to /loop:
#   /loop $(tail -1 MOBILE_LOOP.sh)
#
# Ledger:  docs/MOBILE_APP_ROADMAP.md   (faults F1-F12, doctrine, MB-1..MB-15)
# Target:  https://market-ui-self.vercel.app on a 390px phone
# Scope:   layout and presentation ONLY. Every data path is finished and
#          verified by its own ledger (AI_TRADING_AGENT_ROADMAP DX-1..16,
#          DEXTER_DESIGN_ROADMAP DD-1..14, TN_VISUAL_PARITY, CRYPTO_V*).
#          A mobile task that seems to need different data needs a different
#          LAYOUT, not a different payload — never edit a service, a fetcher,
#          an API route, or a scoring function from this loop.
# Baseline: 857 vitest passing / 0 failing / 7 skipped; tsc 0 errors.
Read docs/MOBILE_APP_ROADMAP.md and follow its doctrine, hard constraints and stop conditions exactly. Do only the first unchecked [ ] task in Section 7; the task's spec text is the requirement and the Section 6 regression rows it names are the acceptance tests. TRUTH: never invent a token, class, prop, breakpoint or number — read the actual anchors in Section 2 first (src/index.css:15-31 for --bg/--surface/--surface-2/--line/--line-strong/--text/--text-2/--text-3/--text-4/--accent/--accent-ink/--up/--down/--flat and the 4pt spacing steps and the radii --radius-sm 2px/--radius 4px/--radius-lg 6px, tailwind.config.js:7-61 for the colour names and the Archivo/Archivo Narrow/Martian Mono families and the label-11px/data-12px/body-13px scale and the radii capped at xl=8px and the stock sm640/md768/lg1024 breakpoints, src/components/AppLayout.tsx for the shell, src/lib/navItems.ts for NAV_ITEMS, src/AppRouter.tsx:163-203 for the 21 routes, src/pages/TradingAssistantPage.tsx:56-57 and :398 and :564-676 and :667 for the trading shell, src/components/trading/{Topbar,Sidebar,AssetInfoPanel,CommunityPanel,Markets,MarketList,ResizeHandle}.tsx, src/hooks/use-mobile.ts for useIsMobile at 768px, src/components/ui/drawer.tsx for the vaul sheet) and confirm a class or prop exists before you render it. MOBILE IS A SECOND LAYOUT, NOT A SHRUNKEN FIRST ONE: a 390px phone is not a narrow desktop, so panels that sit side by side on desktop become destinations on a phone — a sheet, a tab, a route — never 94px slivers, exactly one primary surface owns the viewport below md, and any task whose answer is "make it smaller" is the wrong answer. DENSITY SURVIVES, CRAMPING DOES NOT: keep the Bloomberg-lineage terminal look (11/12/13px type, 2-6px radii, mono numerals, the oklch palette) and do not mobile-ify into rounded cards and 16px body text — the ONE size exception is that every text input must compute to >=16px or iOS zooms the viewport on focus and never zooms back. THE THUMB IS THE POINTER: primary actions live in the bottom third, nothing interactive is under 44x44 CSS px including hit-slop (prefer a transparent inset expansion over visually enlarging the control), and title= is not an affordance on touch so a glyph that needs a tooltip needs a label. HONESTY OUTRANKS POLISH: every rule from docs/DEXTER_DESIGN_ROADMAP.md still binds, so a citation stays one tap from its claim, an uncited figure stays marked where it sits, a fabricated citation stays red, no responsive collapse may hide provenance to save vertical space, and collapsing a panel never licenses inventing a summary of it — hide it or render what the server actually sent. DESKTOP IS A NO-OP: above md every diff must change nothing — the desktop web app is finished and correct, and a user on a laptop must not be able to tell this loop ran. Row 18 is the proof, not your recollection: 1440px full-page screenshots of all 10 routes AND of the modal/drawer open states (asset modal on /trading, citation drawer on /search) identical to the MB-2 baseline. This outranks tidiness — if a mobile task tempts you to also fix an off-scale radius, a stray hex, or a spacing wart that is visible on desktop, DO NOT: note it in Section 8 and move on, because an unrequested desktop change is a regression even when it is an improvement. TOKENS ONLY in new code: no hex literal, no text-[Npx], no rounded-2xl (16px is off-scale — the config only overrides sm/DEFAULT/md/lg/xl), and no prose-* anywhere (@tailwindcss/typography is NOT installed so every prose class compiles to nothing); legacy hex may stay unless a task names it; grep your own diff for those four patterns before you call a task done. REUSE THE SPINE: vaul + components/ui/drawer.tsx, hooks/use-mobile.ts, motion/AnimatePresence, lucide-react, react-resizable-panels, embla-carousel-react and @radix-ui/* are already installed with zero product adoption — extend them, never fork a parallel component, and add NO npm dependency without reading package.json first. HARD CONSTRAINTS: Vercel Hobby caps functions at 12 and apps/market-ui/api is full (agent/ crypto/ social/ tn/ _sina financials fundamentals history news quote spark) so add NO new API route — this is a layout loop and should need zero server work; do not touch apps/market-ui/vercel.json (the /api/((?!tn/|agent/).*) Fly rewrite and the SPA fallback are both load-bearing); Vercel Node ESM needs explicit .js extensions on relative imports from src/ reachable by api/ (builds fine, 500s at request time); erasableSyntaxOnly forbids constructor parameter properties; only DEEPSEEK_API_KEY is live (Gemini quota-dead, Anthropic 401, Groq 401) and VITE_API_URL is commented out in .env.production so market-server is unreachable from prod — mobile work must not depend on it; the prod alias is https://market-ui-self.vercel.app and playwright.config.ts already defaults its baseURL there. A task is done only when: its numbered Section 6 rows are green via npx vitest run and npx playwright test (from apps/market-ui/), npx tsc --noEmit -p tsconfig.app.json is 0 errors, ALL existing suites stayed green (857 vitest passing at ledger open — dexter*, taLevels, drawGate, gridTrust, gridTrustRunner, gridLessons, gridRunStore, gridTrace, gridResearch.sources, EdgarLink, tnColumn* — plus the existing e2e specs featureContinuity, floatingHeader, hubAssetMarket, tradingToSearch, scrollChrome, dexterShip, dexterNarrow, tnColumnAudit, tnNullFeed, gridTrust, backgroundActivity), vercel --prod ran from the repo root (project market-ui, never a preview), and you loaded the live alias at a REAL 390px viewport and measured the change from the actual rendered page (paste the measured numbers into the log — documentElement.scrollWidth vs clientWidth, the chart's measured width as a percentage of viewport, the escapee list, the sheet height in dvh — a fixture proves the component, only prod proves the product). Then flip the ledger line to [x], append ONE Section 8 Progress-log line with REAL numbers (test counts, measured pixels, route names, status codes — no adjectives), and commit on roadmap/world-class with message "feat(mobile): MB-n <what> — <verify numbers>" (git commit -F file if the message has quotes — rtk mangles them). Never git push unless asked — a push to main clobbers Vercel prod. ESCALATE, DO NOT DECIDE ALONE: halt and ask for any git push or any deploy beyond vercel --prod on market-ui, any new npm dependency, any change to vercel.json, any new API route, any file entering the repo the loop did not write and has not read, any desktop screenshot that legitimately must change, and any fault that turns out to be a data problem rather than a layout problem (out of scope — report it, do not fix it here). CADENCE: this loop works rather than watches — every iteration is edit, test, deploy, verify, log, all performed by you, with no external state to wait on — so schedule the next wakeup at 120 seconds, not at an idle-poll interval. PERSISTENCE: never end the loop on usage/rate limit or 429/overload — stop consuming tokens immediately, ScheduleWakeup 3600s with this exact /loop prompt, and on wake re-read the ledger + Progress log and resume the same task from its last verified step; log partial progress BEFORE long or risky operations; chain hourly wakeups as needed. STOP on any of three, and say which: TARGET (no [ ] remains in Section 7 AND MB-15 actually ran the full Section 6 sweep at 320/390/430/768/1440 with the matrix pasted into Section 8 — checked boxes with an unrun gate is not a target hit), BUDGET (15 tasks or 20 iterations, whichever comes first), or STALL (3 consecutive iterations with no row changing state and no new failure mode — then report which row is stuck and on what, and do NOT widen scope or re-run a green sweep to manufacture activity). A failed gate is a real result: if a row cannot go green, log the measurement that proves it and close the row — never invent an MB-16 to keep the loop alive. If a task is blocked on user-only input (credentials, infra, a device you cannot drive headlessly) say exactly what is needed, ledger-note it, skip to the next unblocked [ ] task, and only if ALL remaining are blocked ScheduleWakeup 3600s so the loop resumes when unblocked.
