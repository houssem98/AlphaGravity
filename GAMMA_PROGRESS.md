# GAMMA loop progress — self-improving design + content

NEXT: G3b

## Tasks
- [x] G0a design rubric gamma-design-v1: 4 structure-judged dims + computeStructureStats
- [x] G0b design baseline over 5 archived v2 reports → eval/out/design-baseline.json
- [x] G1a block library + deterministic section-shape classifier (sectionLayout.ts + tests)
- [x] G1b web renderer honors per-section layouts + stat cards (extractor-only)
- [x] G1c designer proposes per-section layout overrides (enum-bounded, validator preconditions)
- [x] G1d design eval re-run → delta vs G0 (NEGATIVE — layout pass unshipped)
- [x] G2a numeric-series → bounded ExhibitSpec extractor + tests
- [x] G2b web inline-SVG exhibits (no new dep) + PDF reuse
- [x] G2c design eval re-run → delta (NULL BY CONSTRUCTION; budget spent on judge noise floor instead)
- [x] G2.5a ReportTheme tokens (institutional/editorial/mono, enum-selected) — PARTIAL, PDF StyleSheet not themed (see ledger)
- [x] G3a exemplar bank: persist {DesignSpec, score, tone/intent} outcomes
- [ ] G3b few-shot designer with top exemplars; measure iterations + score delta
- BLOCKED G4: vision judging (no vision key) · full-pipeline re-eval + W2a verify (Tavily quota) · insight-dim work (needs re-eval)

## Ledger
(one line per task: task · commit · what changed · MEASURED vs expected effect)
- G0a · 5ac3161 · gamma-design-v1 rubric (visual_hierarchy/scannability/exhibit_readiness/layout_variety) versioned separately from content rubric + computeStructureStats zero-LLM counts · 7/7 tests, typecheck clean. BUG FOUND BY TEST: even-length median took upper value → balanceRatio always 1.0 on 2-section docs (blind); fixed with true median. Roadmap corrected: real gamma.app core = per-section layout selection (rewrote G1), added G2.5 themes
- G0b · 2964f63 · designEval.test.ts scores archived markdown (quota-immune; DESIGN_EVAL_DIR/OUT for later corpora) · MEASURED BASELINE 5/5: visual_hierarchy 7.0 · scannability 6.2 (weakest) · exhibit_readiness 7.8 · layout_variety 7.6. Hard stats: avg para 888 chars (scannability drag), balanceRatio 2.41, 17.2 tables, 309 units/report. Judge rationale converges: "dense prose blocks, needs bullets/visual breaks" + "metrics concrete and chart-ready" → G1/G2 have real headroom on scannability, less on exhibit_readiness. Gotcha: deepseek-v4-flash = reasoning model, low max_tokens returns empty text (cap 3000)
- G1a · dd93046 · sectionLayout.ts: 7-layout whitelist, deterministic classifier, extractor-only StatCandidate (value/label/citation all verbatim), layoutPrecondition gate for G1c, layoutVariety score · 27/27 tests, typecheck clean, zero LLM cost. MEASURED over the 5 archived reports (61 sections): **prose 25 (41%) · stat-row 18 · risk-list 7 · comparison 5 · quote-led 4 · table-heavy 2 · timeline 0**; variety 0.57–0.71 (mean 0.65). Reading: 41% of sections have NO structural shape — the same wall-of-prose the G0b judge scored 6.2 on scannability, now located section-by-section, so G1b knows exactly which sections need work. THREE BUGS FOUND BY MEASURING, not by tests: (1) `comparison` fired on 18/61 because its trigger (prose says "vs") also satisfied its precondition — a gate that checks the trigger checks nothing; precondition is now structural (≥3-col table), dropping it to an honest 5. (2) bold TABLE CAPTIONS (`**Financial Scorecard**`) counted as verdict lines, turning table sections into callouts; a caption is a bold line followed by a table. (3) table-heavy's 0.25 share floor split the two identical "Financial Performance" scorecards (0.19 vs 0.28) into different layouts — floor now 0.15, row count is the substantive test. HONEST LIMITS: `timeline` is 0/61 — no section leads its items with dates, so that block is unexercised by this corpus (kept in the whitelist for G1c, not claimed as working). `quote-led` still over-fires on 2 sections that use bold subheads as structure. Corpus citations are named tags (`[Analyst Synthesis]`, `[BLS]`), NOT the `[n]` form the roadmap assumed — extractor handles both. Exec summaries are largely uncited, so they classify as prose not stat-row: real finding, not a classifier miss
- G1b · 60eddf7 · ResearchReport renders each section through its layout (StatRow / VerdictCallout / ComparisonFrame in SectionBlocks.tsx); buildSectionViews is the pure view model; classification runs on RAW markdown so stat cards keep the report's own citation tags; streaming keeps the old single-document path (a half-written section cannot be classified) · 42/42 tests, typecheck + prod build clean. MEASURED over the 5 archived reports: **71 stat cards, 1 verdict callout, 5 comparison frames — 19/61 sections (31%) gain a block**. TWO DEFECTS FOUND BY MEASURING RENDERED OUTPUT, not by fixtures: (1) 3 of 4 promoted "verdicts" were bold SUBHEADS (`AMD-Specific Risks`, `Near-Term Catalysts:`) or the section heading restated in bold — isVerdictLine now requires an actual assertion (no trailing colon, ≠ heading, ≥5 words or contains a digit); verdicts fell 4→1, and the 1 survivor is genuine (`Conviction Rating: Neutral (Sector Weight)`). (2) stat labels were filler — `4x = "nearly"`, `22% = "representing roughly"`; labels now try the leading clause, fall back to the trailing clause (`22% = "of total revenue"`), and the stat is DROPPED when neither names anything. Also fixed clause-splitting inside decimals (`$2.855 trillion` was labelled `"84x and market capitalization"` — split cut through `22.84x`) and markdown syntax leaking into labels. HONEST LIMITS: value + citation are strictly verbatim, but `label` is now the report's words with markdown stripped — no longer a byte-identical substring (test asserts against the stripped source). Weak labels remain where the sentence itself is vague (`62% = "increased"`). timeline block still unrendered (0 corpus instances — building it would be speculative)
- G1c · 0de3387 · DesignSpec gains `sections: {heading, layout}[]`; sectionLayoutDigest shows the designer each section's current layout AND the layouts its structure can legally take; validator applies three gates (heading exists → layout in enum → precondition passes) and turns rejections into revision feedback; buildSectionViews accepts validated overrides; critic prompt now scores layout fit under hierarchy/restraint · 19/19 pdfDesigner tests (6 new), 137 across design suites, build clean. NOT YET MEASURED — this is plumbing; the delta lands in G1d. Design note: the digest lists the legal set up front so the designer spends judgment choosing rather than guessing at what will be rejected
- G1d · ba765cb · rendered the layout pass to markdown (renderLayoutMarkdown) and scored it with the SAME gamma-design-v1 judge as G0b; 3 encodings, 15 judge calls · **MEASURED REGRESSION, change reverted.** Mean design score vs G0 baseline 7.15: stat-block-as-table **6.10** (layout_variety 7.6→4.8) · stat-block-as-cards **6.10** (variety 5.8) · promote-only-2-densest-sections **6.90** (variety 6.8). Best variant −0.25 mean, within judge noise on n=5, but layout_variety is down in ALL THREE (−0.8 best case) — that part is outside noise and consistent. WHY, per judge rationale (converges across all 5 reports): "every section follows identical table-plus-prose pattern", "repeated numbers slow extraction", "long dense paragraphs". Root cause: **the layout pass ADDS a summary layer without SUBTRACTING prose** — computeStructureStats confirms avgParagraphChars 888→888, unchanged. Promoting figures into 18/61 sections made section openings uniform, which is precisely what layout_variety penalizes. Scarcity (cap 2 stat rows/report) recovered 0.8 of it, proving the mechanism, but never beat baseline. ACTION per honesty rule (regression → revert, W1c precedent): web view reverted to plain markdown, SectionBlocks.tsx deleted. KEPT: sectionLayout.ts classifier + preconditions (measurement infra + the G1c validator gate, which ships no visual change on its own) and renderLayoutMarkdown (the offline scoring harness). SECOND-ORDER FIX folded in: judge flagged "tables poorly structured with sentence fragments as metrics" — labels that are one word (`62% = "increased"`) or start a subordinate clause (`"which we assign a"`, `"where ASPs are up"`) are now dropped. MEASUREMENT CONFOUND FOUND: the judge truncates at 28K chars, so a longer rendered doc loses its tail — the table encoding added ~2.3K chars/report and the judge complained about a "missing Bear Case" that was simply cut off; the card encoding cut this to ~1.7K and scarcity to ~440 chars. ROADMAP CONSEQUENCE: per-section layout selection over UNCHANGED prose cannot lift structure scores. To move scannability (6.2, still the weakest dim) the prose itself has to change — G2 should target paragraph density, not another additive block. Judge server: market-server needs `DEV_AUTH_BYPASS=1 PORT=3002`; eval/out is gitignored so these JSONs are local-only
- G2a · ba37b05 · exhibitExtract.ts: parseCellNumber / parseMarkdownTables / extractExhibits / exhibitValueViolations, reusing the ExhibitSpec type from reportQaGates · 16/16 tests, 129 green across design suites, zero LLM cost. MEASURED over the 5 archived reports: **72 candidate series, 0 verbatim violations**; per report 9-23 series from 1-2 tables. Handles BOTH corpus table shapes by trying each rather than guessing orientation: metric-rows × entity-cols (nvda "Financial Scorecard") and entity-rows × metric-cols (macro bank screen, 10 tickers). Safety: a series needs ≥2 cells agreeing on a unit, mismatched cells are DROPPED not converted (`~$500M` never joins a `$B` series — that would be arithmetic on a reported number), and `N/A` / `<100K` / `—` are refused because plotting 100 for "<100K" asserts something the report never said. TWO PARSER BUGS FOUND BY MEASURING: (1) the unit matcher read the **"t" of the word "to"** in `200 to 300 bps` as the trillions suffix, producing bogus `[T]` / `[$T]` units — every unit alternative now ends at a word boundary; (2) ranges like `30–40%` lost their unit because it trails the SECOND figure — unit is now scanned across the cell while the value stays the first figure stated (no midpoint arithmetic). Also stripped `**` out of axis labels. HONEST LIMITS: ranges plot their lower bound, so a `30–40%` bar reads 30; bare-number series (`Deposit Beta Score (1-5)`) carry unit '' and rely on the title for meaning. NOT YET RENDERED OR SCORED — G2b renders, G2c measures. Given G1d, expect the additive-block wall: charts must EARN their score, and if G2c regresses like G1d did, the same revert rule applies. Note: `src/components/trading/Markets.tsx` currently fails typecheck from the PARALLEL session's in-flight edit (3 errors, all in that one file) — not touched, not committed
- G2b · 49121b4 · ExhibitChart/ExhibitStrip inline SVG (zero new deps — a bar chart is a list of rects); barGeometry lives in the SERVICE layer so the maths is DOM-free testable; ResearchReport renders an Exhibits strip after the body; pdfExport falls back to table extraction only when the NumericClaim store is empty (the case that previously yielded no exhibits at all) · 20/20 exhibit tests, 83 across suites, typecheck + prod build clean. MEASURED over the 5 archived reports: **5/5 reports get 3 charts, 88 bars drawn, 0 gate violations**. Both surfaces are hard-gated on exhibitValueViolations — if ANY bar is not findable in the report text, zero charts render (fail closed, not per-bar). Negative-baseline path exercised twice by real data: nvda Intel −75% (zeroX mid-track) and the macro NIM series all-negative (zeroX at the right edge) — barGeometry was written for that because the corpus demanded it, and a −0 sign artifact was caught by test. HONEST SCOPE: this is a PIXEL change. gamma-design-v1 judges markdown and cannot see an SVG; report.markdown is byte-identical before/after, so a G2c re-run would return the baseline by construction — no structure regression is possible, and no visual improvement is measurable either. That means G2b canNOT be validated the way G1d invalidated G1: any claim that charts look better needs the BLOCKED vision model. What IS measured: coverage 5/5, verbatim integrity 88/88, zero new dependencies. KNOWN WEAKNESS in exhibit SELECTION (not correctness): amd-intc yields "AMD", "Intel", "Spread (AMD – Intel)" as three separate one-entity charts because ranking is by bar count and the column axis is longer than the row axis; a per-metric AMD-vs-Intel comparison would tell the better story. The existing DesignSpec.exhibitPick lever already lets the designer choose, so this is a ranking heuristic issue, not a data issue
- G2c · (no code) · **The designed measurement is impossible, so I did not fake it.** G2b changes pixels only: report.markdown is byte-identical before/after and gamma-design-v1 judges markdown, so re-running it returns the baseline BY CONSTRUCTION — any "delta" would be pure judge nondeterminism relabelled as an improvement. Encoding charts into markdown (the renderLayoutMarkdown trick that made G1d measurable) is also void here: a chart rendered as markdown IS the table it was extracted from, so it would just duplicate an existing table — the exact additive-redundancy pattern G1d proved regresses. So exhibit_readiness could not move, and saying "flat" would misdescribe *why*. INSTEAD spent the 5-judge-call budget on something real: **the judge noise floor**, by re-scoring the IDENTICAL baseline corpus a second time. MEASURED (one paired re-run, 20 paired dim-scores): aggregate mean 7.15 → 7.25 (**+0.10**), largest per-dim drift **0.20** (visual_hierarchy and scannability +0.2; exhibit_readiness and layout_variety **0.00**). But per-REPORT scores are wildly unstable: mean |drift| **1.00 point**, max **3 points** (amd-intc visual_hierarchy 8→5 on unchanged input), only **5 of 20** dim-scores identical. TWO CONSEQUENCES. (1) **This validates the G1d revert with data instead of assumption** — and CORRECTS my own earlier characterisation: I logged G1d's −0.25 mean as "within judge noise", but the measured floor on a 5-report mean is ~0.10–0.20, so −0.25 is roughly 2x the floor (weakly real, not noise), and G1d's layout_variety −0.80 is 4x+ a dim whose own drift here was exactly 0.00 — that regression was unambiguously real. The revert was correct, and on stronger evidence than I claimed at the time. (2) **METHOD RULE for the rest of this roadmap (G3b measures score deltas): per-report deltas are uninterpretable at ±1 to ±3 points; only the 5-report average carries signal, and only above ~0.2.** Single-report before/after comparisons must not be reported as evidence. Caveat: this is ONE paired re-run (n=1 pair), an estimate of the floor, not a distribution
- G2.5a · (see commit below) · reportTheme.ts: 3 total token sets (institutional = current house look / editorial = serif+warm+airier / mono = monospace+near-monochrome+tight), each defining all 11 tokens; DesignSpec gains `theme`, validator clamps to the enum and reports an violation for anything invented, designer prompt gains the lever, defaultDesignSpec uses institutional · 46 tests green incl. 7 new theme + 3 new validator cases, typecheck clean. BUG FOUND BY TEST: `themeTokens()` returned the SHARED theme object, so one surface mutating a token would have changed that theme for every other surface — now returns a copy. CONSUMED, not dead config: ExhibitChart/ExhibitStrip render entirely from tokens (font, ink, rule, surface, radius, row rhythm, fallback accent), with an explicit tone-driven DesignSpec.accent still outranking the theme's accent. **SCOPE I DID NOT DO, deliberately, flagged for the user:** PdfDocument.tsx was NOT themed. It holds 80 `C.` palette refs and 122 `s.` style refs behind a single module-level `StyleSheet.create`, so theming it means converting to `makeStyles(tokens)` and threading the result through every sub-component in a 1085-line file. That is a large, high-churn change to a working user-facing export path, and G2c established the judge is blind to pixels, so it would ship unmeasurable risk. The token set and the DesignSpec field are in place, so that refactor is now mechanical if wanted — say the word and it's a follow-up. MEASUREMENT: none possible and none claimed — markdown is untouched, so per G2c this cannot move the design judge in either direction; the roadmap predicted "expect flat scores" and the honest statement is that it is unmeasurable rather than measured-flat
- G3a · **563a5ef (see COLLISION note)** · designExemplars.ts: addExemplar / topExemplars / loadBank / saveBank / recordDesignOutcome; wired into pdfExport so every completed design loop banks its outcome · 15/15 new tests, 108 green across design suites, typecheck clean. The loop previously started COLD on every report and discarded the spec it had just paid an LLM to produce — outcomes are now kept with the critic score they earned, ready for G3b to seed. SAFETY PROPERTY (tested): the bank stores JUDGEMENTS, not content — `pullQuotes`, `abstract` and `exhibitTitles` are stripped before storage, so an exemplar can never carry one report's sentence into a different report; the design decisions (accent/theme/density/tableDesign) survive. Weak/failed loops are refused at `EXEMPLAR_MIN_SCORE = 6` because learning from a spec the critic rejected teaches the designer to repeat it. Retrieval ranks same-tone above raw score (tone drives accent and voice), theme as tiebreak, and still returns the best available when nothing matches so a new tone gets a seed rather than nothing. STORAGE: localStorage, deliberately NOT Supabase — the bank is worth nothing until G3b proves seeding helps, and a table would be schema to maintain for an unproven idea; corrupt JSON or absent storage degrades to an empty bank because recording must never break an export. NOT YET MEASURED — G3b measures iterations-to-pass and final score with vs without seeding, and per G2c only the 5-report AVERAGE will count as evidence. **COLLISION (shared worktree):** my three G3a files were committed by the PARALLEL session inside `563a5ef "fix(trading): column popover paints over the table again"` — my `git add` succeeded but my `git commit` failed on shell quoting, leaving the files staged, and their next commit swept them up. Content is intact and correct; only the commit message is misleading. Their commit was NOT rewritten: they have a `git revert` mid-sequence (`.git/sequencer` holds two `revert feat(trading)` entries) and rewriting HEAD under an active sequencer risks their work. LESSON: in this shared worktree, never leave files staged — stage and commit atomically, and verify `git diff --cached` is empty before yielding
