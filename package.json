{
    "name": "alphagravity",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "description": "AlphaGravity — Unified Market Intelligence & Gravity Search Platform",
    "workspaces": [
        "apps/*",
        "services/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "concurrently --names \"GRAVITY-API,MARKET-SRV,MARKET-UI\" --prefix-colors \"cyan.bold,yellow.bold,magenta.bold\" \"npm run gravity:api\" \"npm run market:server\" \"npm run market:ui\"",
        "gravity:api": "cd services/gravity-api && .venv\\Scripts\\python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
        "market:server": "npm -w market-server run dev",
        "market:ui": "npm -w market-ui run dev",
        "build": "npm -w shared-types run build && npm -w market-ui run build && npm -w market-server run build",
        "loops": "node scripts/graph-lint.mjs && node scripts/governance.mjs && node C:/Users/unicentrale/.claude/scripts/gate-guard.mjs && node scripts/entitlement-probe.mjs && node scripts/plans-sweep.mjs && node scripts/loop-lint.mjs",
        "loops:test": "node scripts/loop-lint.test.mjs && node scripts/graph-lint.mjs --self-check && node scripts/governance.mjs --self-check && node C:/Users/unicentrale/.claude/scripts/loop-prompt.mjs --self-check && node C:/Users/unicentrale/.claude/scripts/gate-guard.mjs --self-check && node scripts/entitlement-probe.mjs --self-check && node scripts/plans-sweep.mjs --self-check",
        "lint": "npm -ws run lint --if-present",
        "typecheck": "npm -ws run typecheck --if-present",
        "install:all": "npm install && cd services/gravity-api && pip install -r requirements.txt",
        "clean": "rimraf apps/*/node_modules services/*/node_modules packages/*/node_modules node_modules services/gravity-api/.venv"
    },
    "devDependencies": {
        "concurrently": "^9.1.0",
        "rimraf": "^6.0.0"
    },
    "engines": {
        "node": ">=20.0.0",
        "npm": ">=10.0.0"
    }
}