Read docs/VIBE_TRADING_ROADMAP.md and follow its hard rules exactly. Do only the first unchecked [ ] task after reading the files it names. Hard rules: never map an upstream field (stooq CSV, sina quote, OKX ticker) without curling a real payload first and pasting evidence in the Progress log; Vercel 12-fn cap — edit existing api functions only, never add fn files; no new market-ui deps; fallbacks shadow-safe (primary shape unchanged, fallback fills only on failure, response carries a source field); factor formulas only from the cited papers (Amihud 2002, George-Hwang 2004, Jegadeesh 1990, Carhart 1997) with a citation comment; capability claims about Vibe-Trading verified against github.com/HKUDS/Vibe-Trading only. Verify: tsc 0 + market-ui build; API tasks curl prod after deploy with REAL numbers logged; hand-check every factor on BIAT + TINV before blending. Flip to [x], one Progress-log line, commit on roadmap/world-class (git commit -F file if the message has quotes — rtk mangles them). vercel --prod --yes only on [deploy] tasks. PERSISTENCE: never end the loop on a usage/rate limit, 429, or overloaded error — stop consuming tokens, ScheduleWakeup at 3600s with this exact /loop prompt, and on wake re-read the ledger and resume from the last verified step (log partial progress BEFORE long operations; the ledger + Progress log are the recovery state); chain hourly wakeups until work succeeds. Only stop when no [ ] remain, or a task is blocked on something only the user can provide — state exactly what's needed and still ScheduleWakeup 3600s so the loop resumes when supplied.
