#!/bin/bash
# TN Deep History loop — self-paced; new session: /loop $(tail -1 TN_HISTORY_LOOP.sh)

Read docs/TN_DEEP_HISTORY_ROADMAP.md and follow its doctrine and hard rules exactly. Do only the first unchecked [ ] task; if a background backfill for it already ran, verify its output and finish the task from there. TRUTH: every candle/level traces to an exchange source (BVMT REST/official downloads/exchange Grafana DB); NO invented points (the Gemini mock's numbers are BANNED), no interpolation-as-data, no ilboursa/investing scraping; data floor is honest — shorter true chart beats longer fake one. Grafana scans date-bounded (25s timeout precedent). Ingestion via LOCAL backfill scripts writing Supabase Storage blobs incrementally (resume-safe); serverless only reads; NO new serverless files (new fn branches in /api/tn/[fn].ts only); no new npm deps. Crypto path byte-identical. Verify per task: real numbers pasted (row counts, date floors, hand spot-checks vs source) + TN regression (board/intraday/news/history 200s) + crypto audit spot MISMATCH 0 + typecheck 0 and vercel --prod (repo root) only if UI/api changed + flip task [x], one Progress-log line with real numbers, commit on roadmap/world-class (git commit -F file if the message has quotes — rtk mangles them). Never git push unless asked. PERSISTENCE: never end the loop on usage/rate limit or 429/overload — stop consuming tokens immediately, ScheduleWakeup 3600s with this exact /loop prompt, and on wake re-read the ledger + Progress log and resume the same task from its last verified step (ledger + Progress log = recovery state); log partial progress BEFORE long/risky operations (backfills especially); chain hourly wakeups as many times as needed. Only stop for: no [ ] remaining in the ledger, or a task blocked on user-only input (credentials/infra) — then say exactly what's needed, ledger-note it, and still ScheduleWakeup 3600s so the loop resumes when unblocked.
